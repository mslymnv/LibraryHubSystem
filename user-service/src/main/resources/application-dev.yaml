spring:
  datasource:
    url: jdbc:postgresql://user-postgres:5432/user_db
    username: postgres
    password: password

  data:
    redis:
      host: redis
      port: 6379

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest